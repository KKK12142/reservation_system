# 부여여고 박람회 상담 시스템 — 배포 운영 가이드

D-Day: **2026년 5월 14일 (목)** · 단독 운영자용 런북

---

## 0. 사전 준비물 체크리스트

- [ ] Google 계정 (학교 G Suite 계정 권장)
- [ ] 학생 명단 (학번 6자리 + 이름 + 학년/반/번호)
- [ ] 부스 명단 (교과군 × 학년)
- [ ] 단축 URL 서비스 (bit.ly 또는 Naver MyD)
- [ ] 각 교사 태블릿 (Wi-Fi 연결 + 브라우저)
- [ ] 각 교실 전자칠판 (브라우저 사용 가능)

---

## 1. 최초 1회 설정 (D-7 ~ D-3)

### 1-1. 스프레드시트 생성

1. Google Drive에서 새 스프레드시트 생성 → 이름 예: **`2026부여여고_박람회_상담시스템`**
2. 좌상단 메뉴: **확장 프로그램 → Apps Script**
3. Apps Script 에디터가 새 탭으로 열림. 프로젝트명도 의미있게 변경 (예: `박람회예약시스템`)

### 1-2. 코드 파일 4개 복사

Apps Script 에디터에서 파일 4개를 만들고 GitHub의 코드를 그대로 복사:

| Apps Script 파일명 | 종류 | 출처 |
|---|---|---|
| `Code` (이미 존재) | 스크립트(.gs) | `Code.gs` 내용 |
| `student` | HTML | `student.html` 내용 |
| `teacher` | HTML | `teacher.html` 내용 |
| `display` | HTML | `display.html` 내용 |

**주의 — 파일명 `.html` 두 번 안 붙게**: HTML 파일을 추가할 때 이름란에 **`student`만** 입력. `student.html` 입력하면 `student.html.html`로 저장돼서 코드가 못 찾음.

저장: `Ctrl+S` (또는 메뉴 → 저장).

### 1-3. 시트 생성 — `setupSheets` 실행

1. Apps Script 에디터 상단의 함수 드롭다운에서 **`setupSheets`** 선택
2. ▶️ 실행 클릭
3. 처음이면 **권한 승인 팝업** → "권한 검토" → 본인 계정 선택
4. "Google에서 이 앱을 확인하지 않았습니다" 경고 → **고급 → 안전하지 않은 페이지로 이동(본인이 만든 앱이라 안전)**
5. 모든 권한 허용
6. 실행 로그에 `{ ok: true, sheets: [...] }` 떠야 성공

→ 스프레드시트 가서 보면 **7개 시트**가 자동 생성됨:
`students` · `booths` · `reservations` · `consultation_log` · `current_state` · `config` · `blocks`

### 1-4. config 시트 점검

자동 시드된 값 확인:

| key | value | 설명 |
|---|---|---|
| event_date | 2026-05-14 | |
| current_block | 0 | 자동 계산값(수동 변경 X) |
| block_override | (빈칸) | 테스트 시 0~7 입력하면 강제 |
| reservation_open | TRUE | 긴급 차단 시 FALSE |
| no_show_minutes | 3 | |
| pre_reg_max_per_student | 2 | 사전예약 학생당 최대 |
| qr_salt | (랜덤) | **절대 변경/노출 금지** |

---

## 2. 데이터 입력 (D-3 ~ D-1)

### 2-1. 학생 명단 입력

`students` 시트 2행부터 입력. **학번은 반드시 6자리** (`010215` 형식).

| student_id | name | grade | class_no | number | qr_token |
|---|---|---|---|---|---|
| 010101 | 김부여 | 1 | 1 | 1 | (비움) |
| 010215 | 이여고 | 1 | 2 | 15 | (비움) |
| 020403 | 박상담 | 2 | 4 | 3 | (비움) |

**Excel/Google Sheets에서 010215를 입력하면 0이 사라지는 문제 방지**:
- 셀 우클릭 → 셀 서식 → 텍스트(`@`) 미리 지정
- 또는 입력 시 `'010215`처럼 작은따옴표 prefix
- `setupSheets` 실행했다면 student_id 열은 이미 텍스트 포맷으로 잡혀 있어서 보통은 문제 없음

### 2-2. 학번 일괄 보정 — `migrateNormalizeStudentIds`

이미 시트에 입력된 학번 중 0이 떨어진 게 있을 수 있으니 한 번 실행:

1. Apps Script 에디터 → 함수 드롭다운에서 **`migrateNormalizeStudentIds`** 선택 → ▶️ 실행
2. 실행 로그에 `{ ok: true, total_processed: N }` 확인
3. 시트로 돌아가서 학번이 모두 6자리(`010215`)로 보이는지 점검

### 2-3. QR 토큰 일괄 생성 — `backfillQrTokens`

학생 모두에게 QR 토큰 발급 (D-Day 로그인 부담 제거):

1. 함수 드롭다운 → **`backfillQrTokens`** → ▶️ 실행
2. 실행 로그에 `{ backfilled: N }` (N=학생 수)
3. `students` 시트의 `qr_token` 열에 8자리 hex(예: `a3f8b2c1`) 채워짐 확인

### 2-4. 부스 명단 입력

`booths` 시트 2행부터.

#### 부스 ID 명명 규칙 (권장)

`B[공간][학년][과목번호]` — 예: `BA101` = A공간 1학년 국어

| 코드 | 의미 |
|---|---|
| BA101 | A공간 1학년 국어 |
| BA102 | A공간 1학년 영어 |
| BA103 | A공간 1학년 수학 |
| BA104 | A공간 1학년 사회 |
| BA105 | A공간 1학년 과학 |
| BA106 | A공간 1학년 교양 |
| BB201 ~ BB206 | B공간 2학년 6과목 |

#### 입력 예시

| booth_id | subject_group | target_grade | space | room | teacher_am | teacher_pm | active |
|---|---|---|---|---|---|---|---|
| BA101 | 국어 | 1 | A | 국어1교실 | 김국어 | 박국어 | TRUE |
| BA102 | 영어 | 1 | A | 영어1교실 | 이영어 | 정영어 | TRUE |
| BA103 | 수학 | 1 | A | 수학1교실 | 최수학 | 한수학 | TRUE |
| BA104 | 사회 | 1 | A | 사회1교실 | 윤사회 | 강사회 | TRUE |
| BA105 | 과학 | 1 | A | 과학1교실 | 송과학 | 임과학 | TRUE |
| BA106 | 교양 | 1 | A | 교양1교실 | 조교양 | 신교양 | TRUE |
| BB201 ~ BB206 | (2학년 6과목) | 2 | B | ... | ... | ... | TRUE |

⚠️ `active`는 **반드시 대문자 TRUE/FALSE**로 입력 (Sheets 체크박스 X).

⚠️ 공간 C(학생 멘토)는 예약 시스템 사용 안 함 — `booths` 시트에 등록하지 않음.

### 2-5. 학생 안내 자료 만들기 (D-2)

학생에게 배포할 안내문에 들어갈 내용:
- 학생 URL (단축 또는 QR 코드)
- 학번 6자리 입력 안내 (예: 1학년 2반 15번 → `010215`)
- 사전예약 마감 시각: **5/14 (목) 09:30**
- 사전예약 학생당 최대 **2건**
- 당일 추가는 09:30 이후 자유롭게

---

## 3. 웹앱 배포 (D-1)

### 3-1. 새 배포 만들기

1. Apps Script 에디터 우상단 **배포 → 새 배포**
2. 톱니 ⚙ 클릭 → **웹 앱** 선택
3. 설정:
   - **설명**: "v1 - 박람회 본 운영" (자유)
   - **실행 계정**: **나** (스프레드시트 소유자)
   - **액세스 권한**: **모든 사용자** (학교 도메인이 G Suite면 도메인 내로 제한 가능)
4. **배포** 클릭 → 권한 승인(처음 한 번)
5. 발급된 **웹 앱 URL** 복사 (https://script.google.com/macros/s/AKfy.../exec)
6. **이 URL을 절대 잃어버리지 마세요** — 모든 화면이 이 URL 기반

### 3-2. URL 매개변수 정리

같은 URL에 `?` 뒤로 매개변수만 다르게 붙여서 사용:

| 용도 | URL 형태 | 예시 |
|---|---|---|
| 학생용 | `…/exec` (또는 `?page=student`) | `…/exec` |
| 교사 부스별 | `…/exec?page=teacher&booth_id=BA101` | 12개 (부스마다) |
| 전광판 학년별 | `…/exec?page=display&grade=1` (또는 `&grade=2`) | 2개 |

### 3-3. URL 단축 + QR 코드 생성

긴 Apps Script URL은 학생/교사 폰에서 입력 불편. **bit.ly** 또는 **Naver MyD**로 단축:

| 라벨 | 짧은 URL 예시 |
|---|---|
| 학생용 | `bit.ly/byh-stu` → `…/exec` |
| 교사 BA101 | `bit.ly/byh-ba101` → `…/exec?page=teacher&booth_id=BA101` |
| 교사 BA102 | `bit.ly/byh-ba102` → `…/exec?page=teacher&booth_id=BA102` |
| ... (12개) | |
| 전광판 1학년 | `bit.ly/byh-disp1` → `…/exec?page=display&grade=1` |
| 전광판 2학년 | `bit.ly/byh-disp2` → `…/exec?page=display&grade=2` |

**QR 코드 생성**: [qr-code-generator.com](https://www.qr-code-generator.com/) 또는 한국어 무료 서비스. 짧은 URL을 QR로 바꿔서:
- 학생용 QR → 안내문에 인쇄
- 교사 부스 QR → 각 태블릿에 붙여 두면 매일 아침 스캔만으로 부스 자동 진입
- 전광판 URL → 직접 입력 (전자칠판 키보드로)

---

## 4. 사전 모의 운영 (D-1)

### 4-1. 시뮬레이션 시나리오

테스트 학생 3명 + 부스 1~2개로 다음 흐름을 처음부터 끝까지:

1. config 시트의 `block_override`를 **`0`**으로 (사전예약 모드 강제)
2. 학생 A 폰에서 학생 URL 접속 → 로그인 → 사전예약 (수학 1학년, 1블록)
3. 학생 A: "사전예약 2건 제한" 검증 (3건째 시도 → 거부 메시지)
4. config의 `block_override`를 **`1`**로 (1블록 진행 중 강제)
5. 교사 태블릿(또는 다른 브라우저)에서 교사 URL 접속 → 부스 BA103 진입
6. 교사: "다음 학생 호출" → 학생 A가 calling 상태
7. 학생 A 폰의 "내 예약 현황"에서 "🔔 호출됨" 표시 확인 (5초 내)
8. 학생 A의 "내 QR 보기" 화면을 교사 태블릿에 보여줌
9. 카메라 작동 → 환영 모달 → IN_PROGRESS
10. **카메라 안 되면** "⌨️ 학번 직접 입력" 클릭 → `010215` 입력 → 같은 결과
11. 교사: "상담 완료" → IDLE
12. 전광판 URL 접속해서 위 변화가 모두 5초 내에 반영됐는지 확인
13. 학생 B를 호출했다가 노쇼 처리 → 다음 호출 시 다른 학생이 와야 함 (큐 끝으로 이동 검증)
14. config의 `block_override`를 **빈칸**으로 (자동 계산 복귀)

### 4-2. 사전 점검 체크리스트

- [ ] 모든 학생이 학생 URL로 로그인 가능 (랜덤 5명 선택해서 직접 학번·이름 확인)
- [ ] 모든 부스 태블릿이 정상 부스 진입 (12대 모두)
- [ ] 카메라 작동 여부 확인 (안드로이드/iOS 따로 점검) — 안 되면 학번 직접 입력 사용 결정
- [ ] 전자칠판에서 전광판 URL 정상 표시 + 5초 내 갱신 확인
- [ ] 학생 명단·부스 명단·qr_token 모두 채워져 있음
- [ ] config.reservation_open = TRUE
- [ ] config.block_override = (빈칸) — 자동 계산 모드
- [ ] 시트 공유 권한 확인 (배포한 본인이 편집자, 그 외 노출 X)

### 4-3. 학생 사전예약 오픈 안내 (D-1 저녁)

학생들에게 단축 URL/QR 배포 + 다음 안내:

> 📅 **2026 박람회 사전예약 오픈**
>
> ✅ 사전예약: **5/14(목) 09:30 이전**까지 본인이 원하는 교과 부스 **최대 2개** 신청
> ✅ 학번 6자리 + 이름으로 로그인 (예: 010215, 김부여)
> ✅ 당일 09:30 이후에는 추가 신청 자유롭게 가능 (1교과군 1건 제한)
> ✅ 본인 차례는 시스템에서 알림 (호출 시 즉시 부스로 이동)

---

## 5. 당일 운영 (D-Day, 5/14)

### 5-1. 09:00 — 시스템 점검 및 워밍업

- [ ] 학생 URL 접속 한 번 (cold start 워밍업, 5초 정도 걸림)
- [ ] 교사 태블릿 12대 모두 켜고 부스 URL 접속 → 운영 화면 정상
- [ ] 전자칠판 모두 켜고 전광판 URL 접속 → **F11 또는 메뉴로 전체화면**
- [ ] 시계가 실시간으로 흐르고 있는지 확인 (네트워크 끊김 신호)

### 5-2. 09:30 — 사전예약 마감 자동 전환

- 시스템이 자동으로 `current_block = 1`로 전환 (시트 변경 없음, 시간 기반 계산)
- 학생 화면 홈 배너가 "사전예약 기간" → "1블록 진행 중"으로 변경됨 자동
- 사전예약 추가 시도 시 거부됨 (당일 추가 모드로 전환)

### 5-3. 운영 중 모니터링

권장: 운영자(영환님) 노트북에서 다음 3개 탭 띄워두고 주시:

1. **시트 직접 보기** — `reservations` 시트로 가서 실시간 추가/완료 흐름 관찰
2. **전광판 URL** (학년 무관)에서 전체 부스 상태
3. **Apps Script "실행 로그"** — 에러 발생 시 즉각 인지

### 5-4. 블록 자동 전환 시각

| 시각 | current_block | 의미 |
|---|---|---|
| ~09:30 | 0 | 사전예약 (마감) |
| 09:30~10:30 | 1 | 1블록 |
| 10:30~11:30 | 2 | 2블록 |
| 11:30~13:30 | 3 | 3블록 (점심 포함) |
| 13:30~14:30 | 4 | 4블록 |
| 14:30~15:30 | 5 | 5블록 |
| 15:30~16:30 | 6 | 6블록 |
| 16:30~ | 7 | 종료 |

운영 중 강제 전환이 필요하면 `config.block_override`에 0~7 입력. 비우면 자동 복귀.

### 5-5. 16:30 — 행사 종료

- 시스템이 자동으로 `current_block = 7` (종료) 상태
- 신규 예약 거부됨
- 진행 중인 상담은 정상 완료 가능
- 마지막 상담 완료 후, `config.reservation_open = FALSE`로 안전 차단

### 5-6. 종료 후 데이터 보존

- `consultation_log` 시트를 **별도 사본으로 백업** (`파일 → 사본 만들기`)
- 사후 분석에 사용: 부스별 상담 건수, 평균 시간, 사전 vs 당일 비율 등

---

## 6. 운영 중 트러블슈팅

| 증상 | 원인 / 해결 |
|---|---|
| 학생이 "학번 또는 이름이 일치하지 않습니다" | `students` 시트에서 학번이 6자리(`010215`)인지 확인. 0이 떨어졌으면 `migrateNormalizeStudentIds` 재실행 |
| 학생 화면에 부스가 안 보임 | 1) `booths.active=TRUE`인지 / 2) 학생 학년에 맞는 부스인지 / 3) 같은 교과군 활성 예약 있는지 |
| 카메라 안 열림 (iPad/iPhone) | 정상. iOS WebKit + Apps Script iframe의 알려진 한계. **"⌨️ 학번 직접 입력"** 버튼 사용 |
| 학생이 호출됐는데 본인 화면에 안 뜸 | 폴링 5초 대기. 그래도 안 뜨면 새로고침 |
| 큐가 안 줄어듦 | 교사가 "완료" 안 누른 경우 다수. `current_state` 시트 확인해서 진행 중인 것 정리 |
| 전광판 멈춤 | 5초 폴링 실패 누적 → 빨간 배너 표시. 새로고침 또는 네트워크 점검 |
| 사전예약이 막힘 | `config.reservation_open` 확인 → TRUE 변경 |
| 동일 학번 중복 예약 | 같은 교과군은 코드가 막음. 다른 교과군은 의도된 동작 |
| 전자칠판이 자동 새로고침 안 됨 | 폴링은 자동. 화면 보호기/슬립 비활성화 확인 |

### 응급 차단

전체 시스템 정지가 필요하면 시트의 `config.reservation_open`을 **FALSE**로 → 신규 예약 즉시 차단. 이미 잡힌 예약·진행 중 상담은 그대로 진행됨.

### 폴백 플랜 (전체 다운 시)

- **부분 다운** (특정 부스만): 해당 부스 `active=FALSE` 후 종이 대기표 운용
- **전체 다운**: 미리 준비한 구글폼 + 종이 대기표로 전환. 학생들에게 공지

---

## 7. 코드 수정 후 재배포

행사 중 코드 변경이 생기면(권장 X), 새 배포가 필요:

1. Apps Script 에디터에서 코드 수정 후 저장 (`Ctrl+S`)
2. 배포 → **배포 관리** → 활성 배포 옆 **✏️ 편집**
3. 버전 드롭다운 → **새 버전**
4. 설명에 변경 내용 적기
5. 배포 → URL은 **그대로 유지됨** (재배포 후에도 동일 URL)

⚠️ "새 배포"가 아니라 "기존 배포 수정 → 새 버전"이어야 URL 보존됨. "새 배포"는 새 URL 발급하므로 학생들에게 다시 공지해야 함.

---

## 8. 부록

### 8-1. 시트 컬럼 참조

#### `students` 시트
| 컬럼 | 타입 | 비고 |
|---|---|---|
| student_id | text(6) | 학번 6자리 |
| name | text | |
| grade | int | 1, 2 |
| class_no | int | 1~5 |
| number | int | 1~99 |
| qr_token | text(8) | `backfillQrTokens`로 자동 |

#### `booths` 시트
| 컬럼 | 타입 | 비고 |
|---|---|---|
| booth_id | text | PK, 부스 고유 ID |
| subject_group | text | 국어/영어/수학/사회/과학/교양 |
| target_grade | int | 1, 2 |
| space | text | A, B (C는 미사용) |
| room | text | 표시용 |
| teacher_am | text | 표시용 (기능 X) |
| teacher_pm | text | 표시용 (기능 X) |
| active | bool | TRUE/FALSE 대문자 |

#### `config` 시트 키 참조
| key | 평소 값 | 비상 시 변경 |
|---|---|---|
| event_date | 2026-05-14 | 변경 X |
| current_block | 0 | 자동 계산. 보지 마세요 |
| block_override | 빈칸 | 0~7 입력 시 강제 |
| reservation_open | TRUE | FALSE로 응급 차단 |
| no_show_minutes | 3 | 변경 X |
| pre_reg_max_per_student | 2 | 변경 X |
| qr_salt | (랜덤) | **절대 변경 X** |

### 8-2. URL 매개변수 참조

| 매개변수 | 값 | 효과 |
|---|---|---|
| `page` | student/teacher/display | 화면 선택 (없으면 student) |
| `booth_id` | BA101 등 | 교사 화면 부스 자동 진입 |
| `grade` | 1/2 | 전광판 학년 필터 |

### 8-3. Apps Script 함수 참조 (운영자 직접 실행)

| 함수 | 용도 | 실행 빈도 |
|---|---|---|
| `setupSheets` | 시트 7개 + config + blocks 시드 생성 | 최초 1회 |
| `migrateNormalizeStudentIds` | 학번 6자리 보정 | 학생 명단 입력 후 1회 |
| `backfillQrTokens` | QR 토큰 일괄 발급 | 학생 명단 입력 후 1회 |

### 8-4. 시트 백업

- 행사 종료 후: 파일 → 사본 만들기 → 별도 폴더 보관
- 행사 중에는 `consultation_log` 시트만 따로 복사해 일자별 백업도 가능

---

## 9. 일정 요약

| 시점 | 작업 |
|---|---|
| D-7 | 스프레드시트 + Apps Script 셋업, `setupSheets` 실행 |
| D-3 | 학생/부스 데이터 입력, `migrateNormalizeStudentIds`·`backfillQrTokens` 실행 |
| D-2 | 웹앱 배포, 단축 URL/QR 발급, 학생 안내문 작성 |
| D-1 | 모의 운영(2시간), 사전 점검 체크리스트, 학생 사전예약 오픈 |
| D-Day 09:00 | 시스템 워밍업, 12개 태블릿/전자칠판 점검 |
| D-Day 09:30 | 자동으로 1블록 시작, 사전예약 마감 |
| D-Day 16:20 | 마지막 블록 종료 |
| D-Day 16:30 | 시스템 자동 종료, `consultation_log` 백업 |

---

문의·이슈 발생 시: GitHub Issues 또는 본 시스템 운영자(영환님)에게 직접.
